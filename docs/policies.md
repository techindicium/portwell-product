# Policies

The company's rules, as written and circulated. Ownership sits with the named person.

Nothing in this repository enforces most of what follows. A policy here is a statement of what
should happen, not a control that makes it happen.

<!-- SEED: replace this block with the policies that reach this repository, copied verbatim
     from the canonical wording, plus the local rules that exist only as team habit. -->

## How to read the status column

| Status | Means |
| :- | :- |
| `current` | In force |
| `draft` | Written, not agreed, and therefore not a gate |
| `superseded by X` | Still reachable in the data, which is a hazard rather than an error |

A `superseded` policy that is still cited by an artifact is a stale-context finding. Do not
delete the superseded version; record the finding.
